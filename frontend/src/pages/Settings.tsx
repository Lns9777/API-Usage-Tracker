import { PageState } from '../components/ui/PageState'

export default function Settings() {
  return <PageState title="Settings" description="Workspace settings and notification preferences will appear here." />
}
