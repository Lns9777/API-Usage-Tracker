import { PageState } from '../components/ui/PageState'

export default function ProjectDetails() {
  return <PageState title="Project Details" description="Project drilldowns will show cost, tokens, and requests over time." />
}
